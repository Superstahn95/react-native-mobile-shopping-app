import { StyleSheet, Text, View } from "react-native";
import SignupSuccessScreen from "@/screens/sign-up/success.screen";

export default function success() {
  return <SignupSuccessScreen />;
}

const styles = StyleSheet.create({});
